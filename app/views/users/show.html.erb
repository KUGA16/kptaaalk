<!-- マイページ -->
<div class="row">
  <%= render 'profile', user: @user %>
  <div id="group_lists">
    <%= render 'group_lists', user: @user, is_notification: @is_notification, invited_groups: @invited_groups, join_groups: @join_groups %>
  </div>
</div>
